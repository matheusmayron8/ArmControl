<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.mayron.matheus.armrobotcontrol">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_PRIVILEGED" />

    <application
        android:name=".Bluetooth.GerenciadorBluetooth"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".InitialActivity"
            android:label="@string/app_name"
            android:launchMode="singleInstance"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ArmControlActivity" android:launchMode="singleInstance"/>
        <activity
            android:name=".Bluetooth.Conectar"
            android:launchMode="singleInstance"
            android:label="@string/title_activity_conectar"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".save.ListMovimentWithSave" android:launchMode="singleInstance"/>
        <activity android:name=".history.HistoricoMovimentos" android:launchMode="singleInstance"/>
    </application>

</manifest>